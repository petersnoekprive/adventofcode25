# adventofcode25

## opzetten ontwikkelomgeving op MacOS Tahoe 26.1 (dec '25)
- lees een tutorial voor `pyenv` zoals deze: (https://realpython.com/intro-to-pyenv/#preparing-macos-systems-for-pyenv) of deze (https://github.com/pyenv/pyenv)
- check of pyenv is geinstallleerd via `pyenv --version` (resultaat: `pyenv 2.6.13`). Installeer pyenv zo nodig via brew.
- check of er een python versie aanwezig is via `pyenv versions` (resultaat: `* system (set by /Users/petersnoekhu/.pyenv/version)` en `3.14.0`). Installer zo nodig een extra versie via `pyenv install 3.14.0` 
- ga naar de map waarin je aan de slag wilt (bijvoorbeeld: `cd /Users/petersnoekhu/Python/adventofcode25`)
- activeer pyenv via 